/*import { rxResource } from "@angular/core/rxjs-interop";

export function createResource<T>(loaderFn: () => Promise<T>) {
  return rxResource<T>({
    request: () => ({}),
    loader: ({ request }) => loaderFn()
  });
}
*/